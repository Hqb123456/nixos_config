#include <bits/stdc++.h>
using namespace std;
bool a,b,c,d;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    cin>>n;
    for(int i=1;i<=n;i++){
        string s;
        cin>>s;
        for(int i=0;i<s.size();i++){
            if(s.find(1)!=string::& s.find(0)!=string::npos){

            }
        }
    }
    return 0;
}
